[pytest]
addopts = -v --cov=app --cov-report=term
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session

#test diario (Todos los días, mientras trabajas)
#pytest

#
#test reporte (Para reuniones con socios o fechas importantes)
#pytest --cov=app --cov-report=html